# Wavly — Dependencies
# Python 3.10 or 3.11 required (NOT 3.12 — no MediaPipe wheel)
# Install order matters:
#   pip install protobuf==3.20.3 numpy==1.26.4 mediapipe==0.10.14
#   pip install opencv-python==4.10.0.84 pyautogui==0.9.54 PyQt6==6.7.0 scikit-learn==1.5.1 pillow==10.4.0 pyperclip

# Computer Vision
opencv-python==4.10.0.84
mediapipe==0.10.14
protobuf==3.20.3        # Must be 3.x — 4.x breaks mediapipe bindings

# System Automation
pyautogui==0.9.54
pyperclip                # Clipboard-based typing — handles all unicode characters

# UI
PyQt6==6.7.0

# ML
scikit-learn==1.5.1
numpy==1.26.4            # Must be 1.x — 2.x breaks mediapipe C extensions

# Utilities
pillow==10.4.0           # Required by PyAutoGUI on Windows